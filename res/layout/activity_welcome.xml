<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/welcome_logo" >

    <ImageView
        android:id="@+id/cit_img"
        android:layout_width="180dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="130dp"
        android:background="@drawable/cit_logo" />

    <TextView
        android:id="@+id/collection_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/cit_img"
        android:layout_centerHorizontal="true"
        android:paddingTop="8dp"
        android:text="@string/connect_networth"
        android:textColor="#FFE856"
        android:textSize="16sp" />

</RelativeLayout>